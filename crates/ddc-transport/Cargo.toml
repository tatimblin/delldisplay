[package]
name = "ddc-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "I2C transport for DDC/CI (macOS IOAVService)."
publish = false

[features]
# Fake transports and the U4323QE fixture for tests in other crates.
testing = ["ddc-core/fixture"]

[dependencies]
ddc-core = { path = "../ddc-core" }

[dev-dependencies]
ddc-core = { path = "../ddc-core", features = ["fixture"] }
